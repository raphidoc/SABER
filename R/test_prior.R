# testthat test for priors

# library(testthat)
# library(SABER)
#
# test_that("make_prior returns valid functions", {
#   data <- rweibull(100, shape = 2, scale = 5)
#   prior <- make_prior(data, "weibull")
#   expect_type(prior$density(1), "double")
#   expect_length(prior$sampler(10), 10)
# })
